{% set bodyClass = 'shows' %}

{% extends "_layouts/site" %}

{% block main %}

{% set showsQuery = craft.entries().section('shows')%}
{% set shows = showsQuery.all() %}
    
<div class="grid-x grid-padding-x grid-padding-y">
  {% for show in shows %}

    {% for act in show.acts %}
    <div class="small-12 medium-4 large-4 xxlarge-3 cell">
      <a href="{{ show.url }}">
      {% set image = act.image.one() %}
      <div class="image--default acts" data-interchange="{% include "_includes/images/contained/7-5/interchange-img-small-12-medium-4" %}">
  
      <div class="content grid-y align-center">
        <div class="shrink cell padding-1 h2 sans-display white text-center">
        {{act.title}}
        </div>
      </div>
  
      </div>
      </a>
    </div>

    {% endfor %}

  {% endfor %}
</div>

{% endblock %}